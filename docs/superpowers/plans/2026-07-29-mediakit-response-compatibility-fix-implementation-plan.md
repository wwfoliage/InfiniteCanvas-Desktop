# AI MediaKit 响应兼容修复实施计划

日期：2026-07-29

## 1. 后端响应解析

- 在 `main.py` 中将 HTTP/业务错误校验与响应数据选择拆开。
- 保留完整顶层响应，兼容顶层、`result`、`data`、`output` 字段。
- 创建任务从候选对象读取 `task_id` / `id`。
- 查询任务从候选对象读取状态、错误原因和视频地址。

## 2. 安全重试

- 后端仅允许“旧解析器误判且无远端任务 ID”的失败记录以原 `client_token` 再提交。
- 进入重试后先把同一条记录改为 `uploading`，阻止并发重复提交。
- 智能画布和普通画布只针对该解析错误复用节点已有 `client_token`。

## 3. 测试

- 增加顶层创建响应、嵌套兼容响应、`success:false`、顶层查询响应测试。
- 增加同 `client_token` 安全重试与非重试状态防重复测试。
- 运行 `tests/test_mediakit.py` 全套测试。
- 对修改后的 JavaScript 执行语法检查。
- 使用模拟响应验证现有失败记录的重试路径，不调用真实 API。

## 4. 交付

- 更新 `E:\InfiniteCanvas` 源文件。
- 创建 v3 替换包，包含修改文件和更新说明。
- 校验压缩包内容、源文件哈希和 ZIP SHA256。
