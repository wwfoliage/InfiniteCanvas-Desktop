# Seedance 2.0 模型名兼容与 480P 显示实施计划

1. 为 `isSeedance2VideoModel()` 增加可重复执行的模型名识别测试，覆盖点号、下划线、连字符、无分隔符、厂商前缀、路径前缀和 Fast 等后缀。
2. 增加非 Seedance 2.0 与相邻错误版本的反例，防止过宽匹配。
3. 修改 `static/js/smart-canvas.js` 中的模型名识别函数，不改变现有平台、协议、即梦和土豆排除规则。
4. 更新 `static/smart-canvas.html` 的脚本缓存版本，确保浏览器加载修复后的脚本。
5. 执行 JavaScript 语法检查、Seedance 识别测试和完整 Python 自动测试。
6. 在本地智能画布中选择当前 `seedance2.0` API，确认菜单包含自动、480P、720P、1080P、4K。
7. 确认选择 480P 后设置值和视频请求体仍为 `480p`，不触发真实生成任务。
8. 制作 v5 替换包，记录本次修复，解压回验文件清单和 SHA256。

